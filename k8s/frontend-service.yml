apiVersion: v1
kind: Service
metadata:
  name: healthcare-frontend-service

spec:
  selector:
    app: healthcare-frontend

  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000

  type: LoadBalancer